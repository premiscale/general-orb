description: |

executor: default
resource_class: small
parameters: {}
steps:
  - checkout
  - circleci-cli/install
  - orb-pre-pack
  - persist_to_workspace:
      paths:
        - orb.yml
      root: ./dist